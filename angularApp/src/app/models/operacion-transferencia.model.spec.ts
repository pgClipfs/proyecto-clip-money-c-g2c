import { OperacionTransferencia } from './operacion-transferencia.model';

describe('OperacionTransferencia', () => {
  it('should create an instance', () => {
    expect(new OperacionTransferencia()).toBeTruthy();
  });
});
